{
  "name": "scan-php-files",
  "version": "1.0.0",
  "description": "A simple utility to scan php files.",
  "main": "findUnionTypes.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "start": "node findUnionTypes.js --format=json --directory=./src"
  },
  "author": "Miguel Gasca <mgascam>",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
